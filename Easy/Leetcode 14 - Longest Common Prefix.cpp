#include <iostream>
#include <vector>
using namespace std;

class Solution
{
    string longestCommonPrefix(vector<string> &strs)
    {
        if (strs.empty())
            return "";
        string result = "";

        return result;
    }
};